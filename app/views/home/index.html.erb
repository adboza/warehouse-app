<h1>Galpões & Estoque</h1>
<h2>Galpões</h2>
<% @warehouses.each do |w| %>
  <%= w.name %>
  Código: <%= w.code %>
  Cidade: <%= w.city %>
  Área: <%= w.area%> m2

<% end %>
<%if @warehouses.empty? %>
  <%= 'Não existem galpões cadastrados!'%>

<%end%>